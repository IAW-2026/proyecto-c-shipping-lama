@import "tailwindcss";

:root {
  --background: #8fa18d;
  --foreground: #171717;
  /* Variables lama */
  --green: #8fa18d;
  --green-dark: #6b7f6a;
  --green-light: #b3c2b1;
  --cream: #f0ede4;
  --cream-dark: #e4e0d6;
  --text: #2d2d2d;
  --text-muted: #7a7a7a;
  --text-light: #a8a8a8;
  --white: #ffffff;
  --border: rgba(143, 161, 141, 0.2);
  --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.06);
  --shadow-md: 0 4px 16px rgba(0, 0, 0, 0.08);
  --radius: 12px;
  --radius-sm: 8px;
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
}

/*
@media (prefers-color-scheme: dark) {
  :root {
    --background: #0a0a0a;
    --foreground: #ededed;
  }
}
*/

body {
  background: var(--background);
  color: var(--foreground);
  font-family: Arial, Helvetica, sans-serif;
}

.table-wrapper {
  overflow-x: auto;
}

/* ── SearchBar (compartido entre dashboard y admin) ── */
.search-bar-wrapper {
  display: flex;
  flex-direction: column;
  gap: 0.4rem;
}

.search-bar {
  position: relative;
  display: flex;
  align-items: center;
  background: var(--white);
  border: 1.5px solid var(--border);
  border-radius: var(--radius);
  padding: 0 1rem;
  transition: border-color 0.2s, box-shadow 0.2s;
  box-shadow: var(--shadow-sm);
}

.search-bar:focus-within {
  border-color: var(--green);
  box-shadow: 0 0 0 3px rgba(143, 161, 141, 0.15);
}

.search-icon {
  color: var(--text-muted);
  flex-shrink: 0;
}

.search-input {
  flex: 1;
  border: none;
  outline: none;
  background: transparent;
  padding: 0.85rem 0.75rem;
  font-size: 0.9rem;
  color: var(--text);
  font-family: inherit;
  min-width: 0;
}

.search-input::placeholder {
  color: var(--text-light);
}

.clear-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--cream-dark);
  border: none;
  border-radius: 50%;
  width: 24px;
  height: 24px;
  cursor: pointer;
  color: var(--text-muted);
  transition: background 0.15s;
  flex-shrink: 0;
}

.clear-btn:hover {
  background: var(--green-light);
  color: var(--text);
}

.search-hint {
  font-size: 0.75rem;
  color: var(--text-light);
  margin: 0 0 0 0.25rem;
}

/* ── Pagination (compartido entre dashboard y admin) ── */
.pagination-wrapper {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  padding: 0.25rem 0;
  flex-wrap: wrap;
}

.pagination-info {
  font-size: 0.8rem;
  color: var(--text-muted);
}

.pagination-controls {
  display: flex;
  align-items: center;
  gap: 0.35rem;
  flex-wrap: wrap;
}

.page-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: 34px;
  height: 34px;
  border-radius: var(--radius-sm);
  border: 1.5px solid var(--border);
  background: var(--white);
  color: var(--text-muted);
  font-size: 0.82rem;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.15s;
  font-family: inherit;
}

.page-btn:hover:not(:disabled) {
  border-color: var(--green);
  color: var(--green-dark);
  background: rgba(143, 161, 141, 0.08);
}

.page-btn.active {
  background: var(--green);
  border-color: var(--green);
  color: white;
  font-weight: 600;
}

.page-btn:disabled {
  opacity: 0.35;
  cursor: not-allowed;
}

.page-dots {
  font-size: 0.8rem;
  color: var(--text-light);
  padding: 0 0.25rem;
  user-select: none;
}

@media (max-width: 768px) {
  .pagination-wrapper {
    flex-direction: column;
    align-items: flex-end;
    gap: 0.5rem;
  }
}